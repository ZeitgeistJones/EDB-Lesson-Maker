# Board-enabling K3 — multi-view environments

Stockpile only. No producer wiring. Prefix `be-k3-`.
Art partition: `harvested/board-enabling/multi-view-environments/` (PNG/JPG — **do not git-add**).
Tracked: `scripts/manus/request-be-k3.mjs`, this doc, `inventory-k3.json`.

## Four views (per world)

1. wide establishing
2. doorway / entrance
3. window / partial occluded
4. overhead / plan-ish

Pedagogy: **information asymmetry** — one view hides what another reveals. Not four cinematic angles.

Registration grades: `REG_A` / `REG_B` / `REG_C` / `REG_FAIL` (same-place lock + asymmetry).

## K1 overlap

All 12 worlds twin locked K1 identities (`be-k1-*`). Geometry briefs mirror K1 SAME locks.

## Running totals

| Metric | Count |
|---|---:|
| Worlds planned | 12 |
| Tasks | 1 |
| Sheets downloaded | 1 |
| Worlds graded | 0 |
| REG_A | 0 |
| REG_B | 0 |
| REG_C | 0 |
| REG_FAIL | 0 |
| View cells | 4 |

## Secondary (runway only)

- S1 registered cutaway layers: **deferred**
- S2 time-era same-place: **deferred**
- Note: only after critical K3 healthy

## Waves / worlds

- **k3-01** `be-k3-kitchen` ↔ `be-k1-kitchen` — FIRED — https://manus.im/app/2KJKmrsNGtGBUK9hLD4NFB — spill under table: visible wide+overhead; hidden doorway+window
- **k3-02** `be-k3-bedroom` ↔ `be-k1-bedroom` — unfired — unfired — under-bed gift: visible wide+overhead; hidden doorway+window
- **k3-03** `be-k3-living` ↔ `be-k1-living` — unfired — unfired — side-table remote: visible wide+overhead; hidden doorway+window
- **k3-04** `be-k3-cafe` ↔ `be-k1-cafe` — unfired — unfired — right-booth cup: visible wide+overhead; hidden doorway+window
- **k3-05** `be-k3-shop` ↔ `be-k1-shop` — unfired — unfired — open cash drawer: visible wide+overhead; hidden doorway+window
- **k3-06** `be-k3-library` ↔ `be-k1-library` — unfired — unfired — under-table backpack: visible wide+overhead; hidden doorway+window
- **k3-07** `be-k3-classroom` ↔ `be-k1-classroom` — unfired — unfired — under-desk lunchbox: visible wide+overhead; hidden doorway+window
- **k3-08** `be-k3-workshop` ↔ `be-k1-workshop` — unfired — unfired — hidden sawhorse: visible wide+overhead; hidden doorway+window
- **k3-09** `be-k3-clinic` ↔ `be-k1-clinic` — unfired — unfired — far counter tray: visible wide+overhead; hidden doorway+window
- **k3-10** `be-k3-bus` ↔ `be-k1-bus` — unfired — unfired — mitten under bench: visible wide+overhead; hidden doorway+window
- **k3-11** `be-k3-platform` ↔ `be-k1-platform` — unfired — unfired — far-post suitcase: visible wide+overhead; hidden doorway+window
- **k3-12** `be-k3-greenhouse` ↔ `be-k1-greenhouse` — unfired — unfired — tipped can behind bench: visible wide+overhead; hidden doorway+window

## QA notes

- World is the job: same place identity across four views; asymmetry must read at a glance.
- Prefer 2×2 contact sheet with LARGE cells.
- Stream E: max 1 Manus in-flight under this partition.
- Do not balloon into secondary S1/S2 until critical K3 is healthy and runway remains.

