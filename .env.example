# Copy to .env (gitignored) and fill in. Never commit real keys.
MANUS_API_KEY=
# Optional: default agent_profile when createTask omits it (asset/spike). Review passes manus-1.6 explicitly.
# MANUS_AGENT_PROFILE=manus-1.6-lite
# Vision labeling for contact sheets (scripts/label-sheet.mjs). Cheap Haiku by default.
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-haiku-4-5
